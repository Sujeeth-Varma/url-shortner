spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: myuser
    password: mypass
  jpa:
    hibernate:
      ddl-auto: update
jwt:
  base64key: ${your_jwt_secret}
  expiration: 172800000 # 2days
